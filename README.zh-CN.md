# ✦ Axiom Agent Control Room

[English](README.md) | **简体中文**

> 把一句话交给一组真正会分工的 Agent。Axiom 会判断任务难度、安排合适的 Agent、展示实时进度，并在交付前帮你检查结果。

当前源码版本：**v2.3.0-rc.8**（任务改进、成果交付与预览稳定性候选版） · [版本记录](CHANGELOG.md) · [升级说明](docs/migration-v2.3.md)

[GitHub](https://github.com/sakuraIsNow/axiom-agent-control-room) · [Gitee](https://gitee.com/water-sim/axiom-agent-control-room)

对话、查资料、读文件、制作内容，或让多个 Agent 协作完成一项任务。Axiom 把执行进度、人工确认和最终成果放在同一个工作空间中。

![Axiom 任务台](docs/images/overview.png)

## 🌌 先用一句话认识 Axiom

Axiom 是一个可以自己安排工作的 AI 控制台。

你不需要先学习工作流、节点或调度规则，只要告诉它想完成什么：

```text
“帮我调研几个开源项目，比较优缺点，最后整理成一份可以分享的报告。”
```

系统会自动完成：

```text
理解任务 → 判断难度 → 选择 Agent → 并行协作 → 检查结果 → 交付
```

简单问题不会被强行拆成复杂流程；需要搜索、分析、绘图或复核时，才会增加对应的 Agent。

界面默认英文，右上角可以切换简体中文，刷新后保留选择。用户输入、模型回答和文件内容保持原文。

### ✨ rc.8 有什么新变化？

- 🌱 **做完一件事，再想想下次怎么做得更好**：让 Agent 根据任务记录提出建议，保存后带入新对话试用。由你决定是否发送，不会偷偷更改现有流程。
- 🎨 **画 SVG、做 HTML 作品少一次不必要的确认**：独立成果直接保存到任务文件中；真正修改项目文件时，仍按原有权限和审批规则处理。
- 🪟 **滚动时作品不再闪一下重来**：上下查看对话、输入文字或继续接收回答，不会重启内容没变的 HTML/SVG 预览，也不会重置正在播放的视频。成果仍可复制、下载。

本版延续原有对话、Agent Graph、Nexus 和插件，不重做执行体系，也不把模型提出的建议当成已经验证的提升。

### 🔬 rc.8 之后的开发更新

**可选接入 Jev 辅助分工，原路由完整保留。** Jev 只做有边界的 Agent 和 Skill 选择，执行顺序仍由原大模型 Scheduler 安排；不确定或服务异常时交回原 Router。支持原路由、影子观察和混合接管三种模式，文本、搜索、视觉和绘图模型不变。识别为本地的模型不向 Jev 云端发送内容，企业内网域名可显式配置。本功能仍属实验接入，不声明任意任务准确率或整体提速。[配置、回退与实测记录](docs/jev-routing-integration-20260921.md)。

**复杂任务交付前再核对一次你的要求。** 系统先整理有原文出处的要求，再检查最终答案是否逐项满足；发现遗漏时最多自动修正一次，不重复执行已经完成的工具。仍有缺口就保留草稿，让你补充材料或明确接受部分结果。对话、任务、Nexus 和插件中都能展开“交付检查”，查看未满足项和真实处理进度。

要求改变后，旧验收不会继续生效；重启后也不会仅因数据库字段顺序变化重新生成成果。普通聊天不增加这套流程。这里的“已复核”是模型语义检查，**不是任意复杂任务的事实准确率保证**，人工接受也不会抹掉未验证项。[查看本批范围与测试记录](docs/final-delivery-verification-20260921.md)。

对有明确数据来源的 JSON 数值结果，还会执行服务端计算校验：模型把算式说对了不算数，实际数值也要一致。暂限已声明的加减乘除和字段，不冒充通用数学或外部事实验证。

此前的本地门禁、24 个确定性业务案例和四例真实模型验收见 [上一批收口记录](docs/business-delivery-closure-20260921.md)。所有首轮失败与测试修正都保留，不将小样本通过推广成任意业务准确率。

- **中断后接着做，做完的不重复做**：修复服务中断时把正在执行的 Agent 记成失败的问题，重启后可继续未完成步骤，保留此前成果。
- **简单聊天少等一次调度**：首轮纯对话由 Router 高置信确认、服务端校验通过后，可省略 Scheduler。附件、历史、纠错和复杂任务仍走完整流程。
- **不只检查分工，也检查交付**：新增 24 个本地业务案例，覆盖报告、附件、文件成果、插件版本、Nexus 和恢复。无密钥 CI 保留每次失败，重试成功也不算稳定通过；另有显式启用、限额的真实模型小样本。[查看范围与验收记录](docs/business-delivery-closure-20260921.md)。

- **Agent 分工更稳**：区分真实 Skill 与能力描述，先校验计划，必要时让 Router/Scheduler 共用一次纠错机会；分别记录模型返回、计划有效、纠错和降级，不把接口成功当成调度正确。
- **建议先比较，再决定用不用**：任务改进中增加五类固定文本样例对照，用同一模型比较加入建议前后的结果；可查看检查证据、原文、耗时和 Token，停止后仍保留记录。

对照使用给定资料和模拟契约，不执行真实搜索、插件修改或 Nexus 流程。样例表现更好不代表所有任务都会变好，也不会自动启用建议。本批不包含登录与私人空间；正式版本号仍保持 rc.8，待下一次发布收口。

## 🧭 前后端结构介绍

- 🎨 **前端**：`src/`，使用 React + Vite + TypeScript（`.tsx`）。
- ⚙️ **后端**：`server/`，使用 Node.js + Hono + TypeScript（`.ts`），负责 API、Agent 调度、模型调用、任务和数据库。

开发时执行 `npm run dev`，前后端会一起启动：前端 `4300`，后端 `8787`。

## 🏆 这个平台的优势

- 🚦 **按需分配 Agent**：简单问题快速回答，复杂任务才启用多个 Agent，减少等待和模型费用。
- ⚡ **能并行就并行**：互不依赖的研究、分析或构建步骤可以同时进行，不必排队一个个完成。
- 🧭 **每轮都会重新判断**：你临时改变想法时，系统只让相关 Agent 参与，不会把整条 Graph 从头跑一遍。
- ✍️ **执行中也能补充要求**：复杂任务还在运行时可以继续输入修改意见，系统会在安全步骤接收并显示“已接收 / 已应用”，不会另建一条重复任务。
- ✅ **交付前会检查**：Reviewer 可以要求修正，结果不是“模型说完成了”就直接交给你。
- 🔄 **中断也能继续**：任务、事件和检查点会保存，刷新页面、网络短暂断开或 Worker 重启后仍可恢复。
- 👀 **过程看得见**：Agent Graph、实时事件和任务阶段来自真实执行状态，不是播放一段固定动画。
- 🔌 **模型可以替换**：默认 DeepSeek，也支持自己的兼容接口；视觉、绘图和视频服务可以单独配置。
- 🛡️ **高风险操作先确认**：修改项目文件、发布等动作按权限等待人工批准；创建独立 SVG、HTML 等任务成果不再因此多一次确认。

## 🧩 已实现功能一览

| 使用场景 | 当前可以做什么 | 使用条件 |
| --- | --- | --- |
| 对话与资料分析 | SSE 流式对话、上下文摘要、图片识别，以及 Markdown、TXT、Word、PDF、SVG、HTML 文件分析和安全渲染 | 配置文本模型；图片理解需配置视觉模型 |
| 联网研究 | 搜索新闻、论文、GitHub 项目和其他实时信息，保留模型返回的来源；搜索失败时不会伪造链接或偷偷切换数据源 | 启用 DeepSeek 原生搜索 |
| 多 Agent 任务 | Router Agent 判断难度，Scheduler Agent 按轮选择研究、分析、构建、复核与汇总 Agent；支持并行 DAG、动态重排和真实 Agent Graph | 内置可用 |
| 人机协作 | 执行中补充要求、暂停、恢复、取消、重试、人工审核、指定 Agent 重跑，以及从 Checkpoint 创建和合并分支 | 内置可用 |
| 复杂任务交付检查 | 原文要求清单、最终答案逐项复核、一次有界修正、缺项草稿与人工部分接受；恢复时检查要求和成果版本 | 文本模型；语义复核不等于独立事实验证 |
| Agent Nexus | 可视化搭建专属 Agent 流，支持条件分支、并行路径、多 Loop、嵌套 Loop、附件、版本发布、运行历史和局部重跑 | 内置可用；多 Worker 附件需对象存储 |
| 插件小程序 | 由 Agent 或用户创建 Mini App，实时预览、持续修改、调整窗口尺寸、发布、审核、回滚、签名、安装和撤回 | 内置可用；外部工具按权限配置 |
| 内容创作与交付 | 对话内生成/编辑图片、通过已配置服务生成视频；回答或整个会话可导出 Markdown、Word、LaTeX 和 PDF 报告 | 绘图和视频分别配置对应模型 |
| 项目与日程 | 项目空间统一管理任务、会话、Nexus、日程、决策、成员和审核；自然语言日程支持时区、依赖、重试和健康建议 | 内置可用 |
| 模板、Agent 与工具 | 工作流模板版本/共享/导入导出，自定义 Agent，MCP/OpenAPI Tool Registry，以及按任务选择的七类能力包 | 内置目录可用；认证型外部工具需凭据 |
| 飞书协作 | 读取云文档、日历和群消息；发送消息进入统一人工审批 | 配置飞书企业自建应用 |
| 记忆、通知与恢复 | 可控长期记忆适配、签名 Webhook 外发、持久 Outbox、任务租约、SSE 回放、断点恢复和多 Worker 数据一致性 | 本地恢复内置；MemoryCore 和多 Worker 需外部服务 |
| 运行与质量观测 | Token、耗时、成本、队列、Worker、模型/工具健康、告警、交付证据、桌面/移动端视觉回归和生产门禁 | 内置可用 |
| 任务改进 · 受控 RSI | 复盘已结束任务、保存建议、进行固定样例对照，再带入新对话试用；后续可延续上一版建议 | 使用来源任务的文本模型；对照需主动发起，可能产生费用，建议不自动生效 |

功能出现在界面中不代表外部服务已经连接。右上角“系统运行状态”会真实探测当前模型、数据库、沙箱、长期记忆和存储状态；未配置的能力会明确显示降级，不会冒充可用。

## 🔄 从不同入口开始，都能接着完成

### 🌱 做过的任务，下次可以怎样做得更好？

在左侧打开 **任务改进**，选择已结束的任务，告诉 Agent 哪里希望改善。它会结合已有结果和你的反馈提出建议。你可以保存、忽略，或把建议带入 **新对话草稿**，检查后再发送。下一次任务结束，也可以沿用上一版建议继续复盘。

这是受控 RSI 的第一阶段，不会擅自更改运行中的 Graph、已发布插件、权限或长期记忆。**保存建议不等于验证有效**；真实效果仍需要对照测试。[查看使用方式与边界](docs/controlled-rsi.md)。

rc.8 后续源码新增 **先对照，再决定**：按给定事实和引用 ID 逐项比较，样例独立于原任务及模型建议的测试题。未知用量不填零；金额和人工干预暂未测量。固定题目公开且可重复使用，不是永久盲测，结果不代表真实业务的普遍提升。

本功能已纳入 **v2.3.0-rc.8**，使用来源任务配置的文本模型复盘，可能产生模型用量。它不执行工具，也不自动测试、发布或启用建议。新对话草稿若会替换未发送文字或附件，会先提示你保留原输入。[查看 rc.8 验收记录](docs/rc8-release-acceptance-20260920.md)。

### 原有执行与协作保持一致

- **选好的模型跟着任务走**：文本、视觉、绘图、视频及原生搜索配置会加密固定下来。恢复任务不会偷偷回到默认模型，新设置也不会改写旧任务。
- **绘图和视频不会盲目重发**：生成与编辑走持久任务；已有受理记录继续查询，无法确认外部操作是否成功时先停下来核对，减少重复生成和重复计费的风险。
- **该你处理时就地处理**：计划确认、工具许可、结果审核和执行核对使用统一入口，对话、任务详情、Agent Nexus 与 Mini App 都能找到。
- **插件里的 Agent 真正按任务分工**：简单问答直接响应，复杂请求进入持久任务。等待审核时保留原请求，不把半成品当作成功交付。
- **状态更可信，界面更统一**：暂停不等于完成，截断不等于完整回答，未返回 Token 用量不伪装成实测零；“需要处理”栏改用现有毛玻璃、克制配色和清晰列表。

普通对话、Nexus 与 Mini App 历史保持隔离，任务管理统一查看执行记录。已有日程继续使用创建时固定的配置；更换凭据前请阅读迁移说明。

内联 Base64 媒体会保存为 Artifact；供应商外链仍可能过期，已完成文件被删除后需从存储备份恢复。自建 API 客户端升级前请查看[媒体接口 HTTP 202 变化](docs/migration-v2.3.md)。

### 任务执行闭环

- **路由暂时不可用，也不丢后半段要求**：前后端使用同一套降级契约，需要搜索、附件、分析或验证时保留对应依赖，不把“查资料并分析验证”变成只有搜索；简单问题仍走轻量路径。
- **减少重复，保留真实用量**：相同交接文本只传一次；审核反复提出相同问题又没有改善时停止自动修正，按原配置进入恢复或人工处理。任务诊断分别记录已知与未知用量、重试、人工介入和交付状态。
- **一次交给多份资料**：图片和文档为本轮补齐对应分析能力，不会把已经安排的研究、分析或构建 Agent 覆盖掉。任务保存这一轮附件的固定副本，避免恢复时读错文件。
- **Agent 能接着工具结果继续做**：先读取、观察结果，再决定下一步查询或操作；每轮都有记录，重复调用或没有进展时会停止，不会无限重试。
- **中断后不盲目重复操作**：已有成功回执可以复用。外部写入结果不确定时，任务详情会提供核对入口，填写核对依据后再明确继续任务。
- **修改过的要求有据可查**：长对话保存有原文来源的约束和决定，跟踪后续修改、替换或撤销；启用工具执行后，拥有 `context.read` 权限的任务 Agent 可以按来源取回原文。提取失败或只覆盖部分内容时，不会假装完整记住。
- **完成不等于事实正确**：执行完成、人工接受、来源可追溯分别显示；工具成功或模型写了“已验证”，都不能自动证明答案中的每个结论正确。

### 执行与交付保障

- **计划先校验再执行**：检查步骤依赖；互不冲突的任务可以并行，重叠写入会排队。
- **中途可以换方案**：补充要求、局部重跑，或从检查点创建分支；状态冲突需要明确处理，不静默覆盖。
- **资料与成果可追踪**：文件、摘要、工具回执和审核意见随任务保存，大结果按需读取，避免反复塞满模型上下文。
- **工具按权限使用**：只选当前任务相关且可用的工具；外部内容不当作系统指令，高风险调用需审批。
- **结果有后续用途**：继续分析、生成报告、保存为可复用流程，或通过签名 Webhook 通知自己的系统。

详细行为与使用边界见[执行闭环](docs/execution-loop.md)、[业务能力](docs/business-capabilities-v2.md)和[工具目录](docs/tool-registry.md)。历史版本变化统一记录在 [CHANGELOG](CHANGELOG.md)。

### 🔁 一次任务的真实执行链路

```text
用户输入
  ↓
Router Agent：理解意图、难度和所需能力
  ↓
Scheduler Agent：选择本轮真正需要的 Agent 和 Skill
  ↓
DAG 计划：校验依赖并计算并行执行波次
  ↓
Researcher / Analyst / Builder / 工具调用
  ↓
Reviewer：检查证据、风险和验收条件
  ↓
Synthesizer：汇总成果、来源和未解决事项
  ↓
带证据的最终交付
```

每轮对话都会重新判断是否需要继续使用旧 Agent、跳过旧 Agent 或加入新 Agent；简单聊天不会被强行升级成完整工作流。

## ✅ 当前版本验收结果

**最新开发源码 · 2026-09-21：**类型检查和生产构建通过；单测 **907 通过、0 失败、18 项 PostgreSQL 条件跳过**，本机无密钥 CI **12/12**。最新隔离全门禁为 **45 通过、1 失败、3 项外部服务跳过**：Nexus Loop 两次有输出但未进入完成状态，仍待定位。视觉 **172/172**、Agent Graph **22/22**、PostgreSQL 稳定性 **20/20**。全门禁早于最后的 Jev 隐私补丁启动，不代表最终源码全链路通过；GitHub 托管结果以 Actions 实际运行记录为准。[查看最新证据与边界](docs/jev-routing-integration-20260921.md)。本次是 **Unreleased 开发同步**，不是新的生产版本发布。

以下为已发布 rc.8 的历史验收，不覆盖上述后续开发结果。

**v2.3.0-rc.8 验收 · 2026-09-20：**最终完整本地门禁为 **41 项通过、0 项失败、3 项外部服务跳过**。

| 检查 | 结果 |
| --- | --- |
| TypeScript 检查与生产构建 | 通过 |
| 单元与运行时测试 | 共 700 项：684 通过、0 失败、16 项 PostgreSQL 条件跳过 |
| 隔离 PostgreSQL 专项 | 18/18 通过，覆盖上述默认命令跳过的数据库场景 |
| 桌面/移动端视觉 · 真实 Agent Graph · RSI 交互 | 172/172 · 22/22 · 17/17 通过 |
| Fake MCP · 路由故障 · 在线路由 · 固定交付质量 | 44/44 · 137/137 · 7/7 · 14/14 通过 |
| 中英文界面 | 通过 |
| 干净源码安装、检查、测试、构建与隔离启动 | 通过 |

**最后一轮完整运行**的可执行项目均首次通过、无重试；此前轮次发现的缺陷、时序问题与修复过程保留在 [rc.8 发布验收](docs/rc8-release-acceptance-20260920.md)，不将整个发布过程称为一次全过。3 项跳过分别是 MemoryCore HTTP、MemoryCore 适配器和 Harness/Codex sidecar，需要配置真实服务，不计为通过。部署 ZIP 解压后的生产依赖安装与隔离启动也已通过，不携带开发者密钥或任务历史。生产依赖审计在检查时为 **0 项已知漏洞**，不等于完整安全认证。

<details>
<summary>上一版本：v2.3.0-rc.7 验收 · 2026-09-07</summary>

```text
npm run check       通过
npm test            644 tests / 630 passed / 0 failed / 14 PostgreSQL skipped
PostgreSQL 隔离专项  16 passed / 0 failed / 0 skipped
npm run build       通过
确定性路由故障      137 passed / 0 failed
在线 Router/Scheduler 7 passed / 0 failed
固定交付质量        14 passed / 0 failed
有数据双语交互      10 passed / 0 failed
真实 Agent Graph    22 passed / 0 failed
npm run qa:visual   172 项断言通过
npm run qa:mcp-business
                    44 passed / 0 failed
npm run qa:all:local
                    37 passed / 0 failed / 3 skipped
```

默认测试跳过的 14 个 PostgreSQL 用例已在独立数据库中补跑。完整门禁自动创建临时 API、另一套 PostgreSQL 专项库及临时 Artifact，不再把测试记录写进日常任务列表；覆盖旧库迁移、Worker 故障接管、MinIO 跨实例读写、Fake MCP 30 个 P0 + 14 个 P1，以及桌面和移动端交互。

首轮完整门禁发现了英文 Readiness 动态告警漏翻译，修复后重新执行全部检查，最终轮没有重试。两轮结果、Graph 性能修复和实际测量边界均保留在[rc.7 验收记录](docs/rc7-product-quality-20260907.md)。固定交付测试只验证明确标注的要求和引用配对，不等于任意回答事实正确；真实复杂任务仍可能需要人工审核或明确接受部分结果，这些不会记作自动首次成功。

仍未现场验收的是 3 项需要额外端点或进程的能力：TencentDB MemoryCore HTTP、Axiom MemoryCore 适配器和 Harness/Codex sidecar。目标部署仍须用自己的 MinIO/S3/COS bucket 重跑对象存储门禁；本机 MinIO 通过不等于云端权限、网络和生命周期策略已经验收。跳过不等于通过，也不影响本地记忆、Artifact 目录和协议级 Harness/Codex 回归。

</details>

受控 RSI 的检查验证权限边界、持久保存、版本冲突、模型输出解析和草稿交接，不证明任意任务的回答质量因此提高。真实模型服务及目标环境的外部依赖仍须单独验收。

## 📈 本机性能基线

下面是 2026-09-04 在 Windows 单节点、25 并发、每项 200 次请求下测得的 API 基线；全部请求均返回 HTTP 200：

| 接口 | 吞吐 | P95 延迟 |
| --- | ---: | ---: |
| 健康检查 | 2,030.49 请求/秒 | 21.38 ms |
| 就绪检查 | 2,493.75 请求/秒 | 11.77 ms |
| 任务列表 | 2,596.16 请求/秒 | 14.78 ms |
| 运行观测 | 1,156.45 请求/秒 | 34.40 ms |

这组数据衡量的是 Axiom 自己的 API、调度和数据库访问，不包含 DeepSeek 的网络延迟、排队时间或模型生成速度。

rc.8 最终 Agent Graph 固定遥测样例中，桌面帧间隔 P95 为 **33.4 ms**，移动端为 **16.7 ms**。此前桌面样本曾超过未调整的 50 ms 门槛，完整记录保留在发布验收文档；这些是本机浏览器测量，不代表模型响应速度，也不把测试清理修复当作产品渲染性能提升。

可以用下面的命令在自己的机器上重新测试 API 基线：

```bash
npm run perf:smoke
```

### ⚡ 为什么使用起来更快？

- 简单问题直接回答，跳过不必要的规划和多 Agent 往返。
- 互不依赖的步骤同时运行，复杂任务的总等待时间更短。
- SSE 会先把“正在分析、正在搜索、正在复核”等真实状态推到页面，不必等整段答案生成完才看到反应。
- 较长的对话会自动整理旧内容，只把当前任务真正需要的上下文交给模型。

所以，平台 API 可以很快响应，但最终答案的生成速度仍会受到模型服务、网络和任务复杂度影响；README 中的基线不会把第三方模型速度算成 Axiom 自己的性能。

## 📦 能力包与飞书

进入“项目空间 → 能力”可以看到七类能力包。新租户首次进入时默认启用开发与代码、研究与论文、办公协作、数据分析；停用状态会持久化，服务重启不会擅自重新开启。能力包决定哪些外部工具可以进入 Router 候选，工具仍要继续通过租户隔离、Agent 权限、健康状态、任务相关度和 Top-K 筛选。

连接飞书需要先在飞书开放平台创建企业自建应用，并准备 `App ID`、`App Secret`。根据实际用途给应用授予云文档读取、日历读取、群消息读取和以应用身份发送消息等权限，然后在“项目空间 → 能力 → 连接飞书”中验证。只有飞书真实返回 `tenant_access_token` 后，页面才显示已连接；读取操作直接执行，发送消息属于高风险写操作，会等待人工确认。

建议在部署环境设置不少于 32 个字符、独立且稳定的 `AXIOM_INTEGRATION_SECRET`。它用于 AES-256-GCM 加密集成凭据；未设置时会回退使用 `AXIOM_PROVIDER_SECRET`，回退密钥也必须满足同样长度。更换密钥前必须先迁移或重新连接现有凭据，否则旧密文无法解密。

## 🗓️ Agent 日程

日程不再只是“每隔多少分钟重复同一句话”。你可以直接告诉日程 Agent：

```text
“每天早上 9 点搜索 Agent 行业动态，整理成 5 条摘要并保留来源。”
```

平台会先生成一份草案，显示任务目标、执行时间和安排理由；只有你点击确认后才会启用。当前支持单次执行、固定间隔、每天固定时间和每周指定日期，并按用户时区计算下一次执行，固定时间不会因上一次任务晚结束而逐渐漂移。

每次真正到点时，平台会重新让 Router Agent 理解本次目标，再由调度 Agent 选择当轮需要的搜索、分析、构建、审核或其他 Agent 和 Skill。日程不会永久绑死某个 Agent，也不会把简单任务强行拆成复杂流程。

如果你在设置中启用了已保存的自定义文本模型，日程草案、到点路由和实际执行会使用同一份加密凭据引用；日程记录只保存凭据 ID，不保存或返回 API Key。

日程页还可以：

- ▶️ 立即运行一次，不改变原来的自动执行时间。
- 🧭 查看本次实际使用的 Agent、执行状态、步骤、Token 和交付验证结果。
- 🗓️ 切换本周或未来 35 天，提前看到哪些任务会在同一时间挤在一起。
- 🩺 发现连续失败、Token 突增或交付质量下降；平台只提出建议，必须由你确认后才会暂停、恢复或调整时间。
- ✅ 查看最近确认过的调整记录；重复点击旧建议或日程状态已经变化时，系统会拒绝执行。
- 🔗 让下一条日程接着使用上一条已经验证的结果，同时固定来源版本，避免内容悄悄变化。
- 🔁 在短暂故障后自动退避重试，连续失败 5 次后暂停并等待人工恢复。
- 🧱 在 PostgreSQL 多 Worker 部署中安全认领到期日程，并用幂等键避免重复任务。
- 🕰️ 继续读取旧版本创建的固定间隔日程，无需手动迁移。

平台不会根据“最近没打开”就猜测某条日程已经没用，也不会自行删除。需要改变执行安排时，页面会把原因和依据放在确认框里，决定权仍在你手里。

## 🪟 界面一览

任务台、对话、Agent Nexus、插件和模型设置的实际界面如下。

### 🛰️ 任务台：知道现在进行到哪一步

左侧进入任务、对话、插件或 Agent Nexus；中间区域显示当前任务和实时 3D 场景；右侧可以看到负责人、阶段、审核状态和交付结果。

### 💬 对话：像聊天一样使用 Agent 团队

![对话与 Agent Graph](docs/images/conversation.png)

你只需要继续说下一句话。系统会根据新的内容重新判断本轮需要哪些 Agent，旧 Agent 可以被跳过，新 Agent 也可以临时加入。任务执行中还可以直接补充要求；对话顶部会显示本轮真实路径，右下角的 Agent Graph 会跟着执行状态变化。点击节点可以查看角色、上游 Agent、Skill、Token、耗时、重试和工具调用；需要更多空间时可以全屏查看，低性能设备会自动减少动态效果。

### 🔗 Agent Nexus：把一组 Agent 组成自己的工作流

![Agent Nexus](docs/images/agent-nexus.png)

可以把“资料搜集 → 分析 → 复核 → 输出”连成一条流程，也可以加入条件分支和 Loop。每个 Agent 都有清晰的职责和输入输出，运行时可以查看每一步发生了什么。

### 🧩 插件：像打开一个小程序

![插件中心](docs/images/plugins.png)

插件可以由 Agent 协助创建，也可以自己配置。发布前会检查外部依赖、直连网络、字段冲突和工具可用性，并列出它申请的平台 Agent/工具权限。每次修改都会保留历史版本，恢复旧版本时会生成一个新草稿，不覆盖现有记录。

团队插件通过管理员审核后才会出现在插件市场。安装会固定到当时审核通过的版本，作者后来修改插件不会在用户不知情时自动替换；用户可以自己决定升级、卸载或恢复到仍然安全的旧版本。某个版本被撤回后，平台会立即停止运行该版本，避免继续使用已经发现问题的插件。

生产部署可以为发布版本生成完整性签名：配置至少 32 个字符的 `AXIOM_PLUGIN_SIGNING_KEY` 后，新发布插件会保存覆盖内容、权限、发布人和时间的 HMAC 签名；再启用 `AXIOM_REQUIRE_PLUGIN_SIGNATURE=true`，运行时只接受校验一致的版本。Prompt 插件运行和 Mini App 打开前都会从服务端重新读取当前版本并复核；内容、权限风险或验签配置变化时会拒绝运行。已有未签名插件需要在“版本与权限”中检查后重新发布。

### ⚙️ 设置：换成你自己的模型

![模型设置](docs/images/settings.png)

默认使用 DeepSeek，也可以填写自己的 OpenAI-compatible 服务地址、模型名和 Key。文本模型、视觉模型、绘图服务和视频服务可以分开配置。

## 🚀 三步启动

### 1. 安装依赖

要求 **Node.js 22 或更高版本**。基础对话可以直接使用 Node.js；要启用隔离工具执行或运行完整生产门禁，还需要先启动 Docker，并准备沙箱镜像。

```bash
git clone https://gitee.com/water-sim/axiom-agent-control-room.git
cd axiom-agent-control-room
npm ci
```

完整门禁前可先检查 Docker：

```bash
docker info
docker image inspect ubuntu:22.04
```

如果第二条命令提示镜像不存在，执行：

```bash
docker pull ubuntu:22.04
```

### 2. 填写自己的模型配置

复制配置模板：

```bash
cp .env.example .env.local
```

Windows PowerShell：

```powershell
Copy-Item .env.example .env.local
```

打开 `.env.local`，至少填写：

```dotenv
DEEPSEEK_API_KEY=填写你自己的Key
```

项目不会附带可用 Key。`.env.local` 已被忽略，不会被 Git 提交。

再为可恢复任务生成稳定的加密密钥：

```bash
node scripts/setup-local-provider-secret.mjs
```

已有密钥不会被覆盖，也不会打印出来。请安全备份 `.env.local`；多 Worker 部署需要统一分发同一个 `AXIOM_PROVIDER_SECRET`，不要每台机器各生成一份。

### 3. 启动

```bash
npm run dev
```

打开：

- 前端：<http://127.0.0.1:4300>
- API：<http://127.0.0.1:8787>

默认使用 SQLite，第一次启动会自动在 `.data/` 创建本地数据库，适合个人试用。

## 🐘 想用 PostgreSQL？

项目附带了一个可以直接使用的本地 Docker 配置：

```bash
docker compose -f docker-compose.local.yml up -d postgres
```

然后在 `.env.local` 设置自己的连接地址：

```dotenv
DATABASE_URL=postgresql://postgres:change-me@127.0.0.1:5432/axiom
```

执行迁移并启动：

```bash
npm run build
npm run db:migrate
npm start
```

构建后的服务在 <http://127.0.0.1:8787> 同时提供前端和 API。SQLite 适合单人本地使用；PostgreSQL 用于多 Worker、任务恢复和受控生产环境。

## 🧰 常用命令

```bash
npm run check       # TypeScript 检查
npm test            # 单元和运行时测试
npm run qa:ci       # 无密钥源码副本：检查、单测、构建和浏览器回归
npm run qa:business-delivery # 24 个跨组件业务案例，不调用付费模型
npm run qa:business-oracles # 独立结果判定器及隔离边界回归
npm run qa:business-live # 显式启用真实模型小样本，可能产生模型费用
npm run build       # 构建前端和服务端
npm run db:migrate  # 使用已构建的服务端执行 PostgreSQL 迁移
npm start           # 运行构建后的单体服务
npm run qa:visual   # 浏览器界面验收
npm run qa:improvements # 受控 RSI 建议、中英文与新对话草稿回归
npm run qa:preview-stability # 滚动、流式输出时保持交互预览稳定
npm run qa:business # 分段业务闭环评测
npm run qa:mcp-business # 本地 Fake MCP 30 个 P0 + 14 个 P1 业务门禁
npm run qa:context-summary # 持久摘要 API 回归
npm run qa:harness-live # 已配置 sidecar 的真实能力握手
npm run qa:object-storage # 已配置 MinIO/S3/COS 时验证跨 Worker Artifact
npm run qa:object-storage:local # 自动准备本机 MinIO 并验收测试桶
npm run qa:postgres:local # 使用一次性 PostgreSQL 数据库演练 Worker 接管
npm run qa:all      # 生产门禁回归
npm run qa:all:local # 本机 MinIO + 临时 PostgreSQL 完整稳定版门禁
npm run release:package # 构建可部署 ZIP，并生成 SHA-256 校验文件
```

生产构建完成后访问 <http://127.0.0.1:8787>，Hono 会同时提供 API 和 `dist/` 中的前端。

## 🔌 可选能力配置

不配置这些服务时，基础对话和本地任务仍然可以使用；对应模块会显示“未配置”或“降级”。

| 能力 | 配置项 | 用途 |
| --- | --- | --- |
| 文本模型 | `DEEPSEEK_API_KEY`、`DEEPSEEK_API_BASE`、`DEEPSEEK_MODEL` | 对话、分析和 Agent 执行 |
| 视觉模型 | `DEEPSEEK_VISION_*` | 图片识别和文件视觉分析 |
| 原生搜索 | `DEEPSEEK_NATIVE_SEARCH=true`、`DEEPSEEK_NATIVE_SEARCH_MODEL` | 联网搜索与来源整理 |
| 绘图 | `DMX_API_KEY`、`DMX_BASE_URL`、`DMX_MODEL` | 生成和编辑图片 |
| 视频 | `VIDEO_API_BASE`、`VIDEO_API_KEY`、`VIDEO_MODEL` | 连接本地视频生成服务 |
| 长期记忆 | `TDAI_MEMORY_ENDPOINT`、`TDAI_MEMORY_API_KEY` | 可选的 MemoryCore L0-L3 记忆 |
| 外部 Agent | `DEEPSEEK_HARNESS_*`、`CODEX_APP_SERVER_*` | 接入 Harness 或 Codex sidecar |
| Nexus 附件 | `AXIOM_NEXUS_ARTIFACT_BUDGET_BYTES` | 单任务可加载的附件总预算，默认 20 MB |
| 外部工具路由 | `AXIOM_EXTERNAL_TOOL_TOP_K` | 每个 Agent 步骤最多注入的相关 MCP/OpenAPI 工具，默认 6 |
| MCP 调用超时 | `AXIOM_MCP_CALL_TIMEOUT_MS` | MCP 单次调用超时，限制在 10ms 至 120s，默认 30s |
| 只读工具重试 | `AXIOM_EXTERNAL_READ_RETRIES` | 只读工具瞬时失败的额外重试次数，默认 2 次，最多 3 次；写操作不重试 |
| 工具审批有效期 | `AXIOM_TOOL_APPROVAL_TTL_MS` | 高风险写操作审批有效期，默认 15 分钟，限制在 1 秒至 24 小时 |
| 集成凭据加密 | `AXIOM_INTEGRATION_SECRET` | 不少于 32 个字符；加密飞书等集成的 Secret，未设置时回退到 `AXIOM_PROVIDER_SECRET` |
| 外发通知 | `AXIOM_NOTIFICATION_SECRET`、`AXIOM_NOTIFICATION_RETENTION_DAYS` | 签名 Webhook、失败重试与投递审计 |

`qa:harness-live` 只有在 sidecar 命令已配置时才执行真实握手；未配置时生产门禁会明确标为跳过。协议模拟测试不能替代目标服务器上的真实任务、断流和跨 Worker 演练。

更完整的变量说明见 [`.env.example`](.env.example)。

外发渠道位于页面右上角的“通知 → 外发通知”。页面支持测试、暂停、编辑、删除和死信重投；Webhook 地址与签名密钥会加密保存，浏览器只会收到脱敏地址。删除渠道会擦除地址和密钥，已脱敏的投递审计按配置的保留期保存。接收方验签与幂等处理见 [`docs/outbound-notifications.md`](docs/outbound-notifications.md)。

## 🛡️ 安全和生产提示

这个开源仓库提供的是“本地或受控单节点生产候选”，不是打开端口就可以直接面对公网的多租户 SaaS。

正式部署前至少需要：

- 使用 PostgreSQL，不要在公网生产环境使用 SQLite。
- 通过 OIDC 或可信反向代理注入用户和租户身份。
- 把模型 Key 放到 Secret Manager，不要写进前端或任务内容。
- 启用 HTTPS、限流、日志采集、备份和对象存储。
- 根据实际环境验收 Docker 沙箱、MemoryCore、Harness/Codex sidecar 和多 Worker 恢复。

具体上线门禁见 [`docs/launch-readiness.md`](docs/launch-readiness.md)。

## 📚 想继续了解

- [当前升级说明](docs/migration-v2.3.md)：任务配置、数据迁移和媒体接口变化。
- [Agent Nexus 控制流](docs/agent-nexus-control-flow.md)：分支、Loop、DAG 和局部重跑。
- [执行闭环](docs/execution-loop.md)：任务如何从输入走到交付。
- [工具目录](docs/tool-registry.md)：MCP/OpenAPI 按需路由、健康、权限、审批和执行边界。
- [v2.2 能力包迁移指南](docs/migration-v2.2.md)：能力包、飞书连接、加密凭据和升级边界。
- [MemoryCore 接入](docs/memorycore-integration.md)：长期记忆配置与验收。
- [业务能力 V2](docs/business-capabilities-v2.md)：15 项业务闭环、数据边界与验收方法。
- [Harness 适配器](docs/harness-adapters.md)：DeepSeek Harness/Codex 的可选接入方式。
- [上下文窗口与持久摘要](docs/context-window.md)：长对话如何压缩、校验并恢复。
- [业务闭环评测](docs/runtime-business-evaluation.md)：分段评测维度和失败定位方式。
- [上线就绪度](docs/launch-readiness.md)：当前能力、风险和生产前置条件。
- [v2.1.0 迁移指南](docs/migration-v2.1.md)：从 v2.0.0 升级二进制附件与 MCP 路由。
- [v2.0.0 迁移指南](docs/migration-v2.md)：从 v1.1.0 升级到 v2 的基础步骤。
- [版本变更记录](CHANGELOG.md)：每个正式版本的新增能力、行为变化和外部依赖。

## 📄 开源许可

本项目使用 [MIT License](LICENSE)。你可以自由学习、修改和部署，但请自行配置模型服务并对生产环境负责。

## 🙌 贡献

欢迎提交 Issue、改进文档或发起 Pull Request。涉及 Agent 执行、安全边界、任务恢复和数据隔离的改动，请同时补充测试或回归脚本。
